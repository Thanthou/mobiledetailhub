/**
 * Re-export service data types from shared
 * This maintains backward compatibility while centralizing type definitions
 */
export type {
  ImageRef,
  VideoRef,
  CTAButton,
  ProcessStep,
  ChartData,
  ServiceData
} from '@shared/types/service.types';

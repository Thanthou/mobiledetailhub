/**
 * Unified Environment Configuration
 * Re-exports the async-safe env loader for backward compatibility
 */

// Re-export from the consolidated async env loader
export { env, loadEnv } from './env.async.js';